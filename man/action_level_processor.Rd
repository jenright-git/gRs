% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/action_levels.R
\name{action_level_processor}
\alias{action_level_processor}
\title{Read an ESDAT action level (guideline) export}
\usage{
action_level_processor(myfile_path, name = NULL, sheet = NULL)
}
\arguments{
\item{myfile_path}{file path to an action level export. More than one path
may be given, in which case the results are stacked.}

\item{name}{name for the guideline set, written to \code{criteria_name}, e.g.
\code{"ANZG 95\% Marine"}. Defaults to the file name without its extension.
Recycled against \code{myfile_path}.}

\item{sheet}{optional sheet name. By default the sheet is found from its
contents, with and without a leading banner row.}
}
\value{
A tibble with one row per action level and the columns
\code{criteria_name}, \code{chem_code}, \code{chem_name}, \code{matrix_code}, \code{criteria},
\code{criteria_unit}, \code{criteria_basis}, \code{criteria_text}, \code{leached}, \code{total},
\code{filtered}, \code{conditions} and \code{comments}, carrying a \code{"report_type"}
attribute of \code{"action_level"}.
}
\description{
ESDAT exports a set of action levels as a flat table of one row per
analyte, e.g. the ANZG marine water toxicant default guideline values.
The export carries no column naming the guideline set itself, so the name
is supplied by \code{name} (or taken from the file name).
}
\details{
The exported value and its unit share one cell (\code{"0.6 ug/L"}, with a real micro sign), so they are
split into \code{criteria} and \code{criteria_unit}. Where the unit records the
basis of measurement - \code{"0.006 ug Sn/L"} for tributyltin as tin, \code{"as N"}
for ammonia - the basis is stripped into \code{criteria_basis} and the unit is
left as the plain concentration unit so it can be converted.

\code{leached}, \code{total} and \code{filtered} record which result types the guideline
applies to, and \code{\link[=join_action_levels]{join_action_levels()}} uses them to decide whether a given
result is comparable: a filtered (dissolved) result is only compared
against a guideline with \code{filtered = TRUE}, a leachate result (\code{REG} vs
\code{LEACHED_REG} in an ESDAT soil export) only against \code{leached = TRUE}.
}
\examples{
\dontrun{
anzg <- action_level_processor(
  "ANZG Marine Water Toxicant DGVs LOSP 95_ (March 2026).xlsx",
  name = "ANZG 95\% Marine"
)

# several guideline sets stacked, then used one at a time
levels <- action_level_processor(
  c("ANZG 95.xlsx", "ANZG 99.xlsx"),
  name = c("ANZG 95\%", "ANZG 99\%")
)
}
}
