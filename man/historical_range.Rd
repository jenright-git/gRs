% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical_range.R
\name{historical_range}
\alias{historical_range}
\title{Compare a monitoring round against the record behind it}
\usage{
historical_range(
  data,
  group_vars = "location_code",
  round_col = NULL,
  round = NULL,
  date_col = date,
  spike_factor = 10,
  keep = c("all", "new_max", "spike", "exceedance"),
  criteria_col = criteria,
  include_criteria = NULL,
  save_path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{chemistry tibble from \code{\link[=data_processor]{data_processor()}}, holding the whole
record and not just the round of interest.}

\item{group_vars}{character vector of columns to group by within each
analyte. Default \code{"location_code"}. Pass \code{character(0)} to summarise each
analyte across all locations. Analytes are always grouped by \code{chem_name},
and by \code{output_unit} and \code{criteria_set} where those are present.}

\item{round_col}{name of the column naming the monitoring round. Can be
given with or without quotes. By default the first of \code{monitoring_round},
\code{task_code}, \code{date} or \code{sampled_date_time} that \code{data} carries.}

\item{round}{the round to report on. Default \code{NULL}, the latest round
present.}

\item{date_col}{name of the date column that orders the record. Can be
given with or without quotes. Default \code{date}, which \code{\link[=data_processor]{data_processor()}}
always returns. Where it is absent, every result outside the round is
treated as history and a warning says so.}

\item{spike_factor}{a new maximum this many times the previous detected
maximum is flagged in \code{spike}. Default 10. \code{NULL} switches the check off.}

\item{keep}{which rows to return. \code{"all"} (default) returns every group;
\code{"new_max"} and \code{"spike"} cut it to the screening list, ordered by
\code{max_ratio} descending; \code{"exceedance"} cuts it to the groups whose current
result exceeded its guideline, and needs a guideline joined on.}

\item{criteria_col}{name of the column holding the guideline value, matching
the \code{value_col} it was joined under by \code{\link[=join_action_levels]{join_action_levels()}}. Can be
given with or without quotes. Default \code{criteria}.}

\item{include_criteria}{include the guideline and whether the current result
exceeded it. Default \code{NULL}, which includes them when \code{criteria_col} is
present.}

\item{save_path}{full file path, including filename, to write the table to.
The directory is created if it does not exist.}

\item{quiet}{suppress the messages naming the round and reporting an empty
result.}
}
\value{
A tibble with one row per group, carrying \code{n_samples} (results held
up to and including the round), \code{n_current}, \code{hist_min_prefix},
\code{hist_min_conc}, \code{hist_max_prefix}, \code{hist_max_conc}, \code{current_prefix},
\code{current_conc}, \code{new_max}, \code{new_min}, \code{max_ratio} and \code{spike}, plus -
with \code{include_criteria} - \code{criteria} and \code{current_exceedance}. A group
with no history reports \code{NA} rather than the \code{Inf} an empty \code{min()} gives;
a group not sampled this round reports \code{NA} for the current result. The
round is attached as a \code{"round"} attribute. Format it with \code{\link[=create_gt]{create_gt()}}.
}
\description{
One row per location and analyte, holding the historical range of results
and the result from the round being reported on, with flags marking the
ones that set a new maximum or minimum. The screening check behind a
"New Maximums" section, and the per-location historical range table that
usually sits beside it, are the same function with different arguments -
\code{keep} chooses between them.
}
\details{
Three conventions decide what the table says. They are not symmetrical, on
purpose:
\itemize{
\item The historical \strong{minimum} is taken over all prior results, detected or
not, so a non-detect can legitimately set a new minimum.
\item The historical \strong{maximum} is taken over prior \strong{detections} only. Limits
of reporting fall over the life of a monitoring program, and an old
\verb{<0.5} would otherwise stand as a maximum that was never measured.
\item The \strong{current} result is the \emph{maximum} of the round, not whichever result
happened to sort first. Where a group holds more than one result for the
round - a field duplicate that was never collapsed - that is a data
quality problem, so the groups are named in a warning rather than one
result being picked silently. \code{\link[=select_max_concentration]{select_max_concentration()}} is the fix.
The warning only applies where the grouping identifies a single sampling
point: grouped across locations, several results in a round is the
ordinary case.
}

A new \strong{maximum} therefore requires a detection on both sides: a \verb{<0.5}
reported this round can never be called a new maximum however high the limit
of reporting of the day happened to be.

\code{max_ratio} is the current result as a multiple of the previous detected
maximum, and \code{spike} marks the ones above \code{spike_factor}. A jump that large
is more often a transcription or a unit problem than a real change, so it is
worth checking against the laboratory report before any text is written
around it.

History is everything sampled \strong{before} the round, so a round taken from
the middle of the record is compared against what preceded it rather than
against results that had not been collected yet. Results sampled after the
round are left out altogether, \code{n_samples} included.
}
\examples{
# every location and analyte, latest round against its own history
historical_range(gRs_data)

# the screening list: this round's detections well above anything before
historical_range(gRs_data, keep = "spike")

\dontrun{
# one analyte, per location, as a report table
chem \%>\%
  dplyr::filter(chem_name == "Copper") \%>\%
  historical_range() \%>\%
  create_gt()

# only where the current result exceeded its guideline
compared \%>\% historical_range(keep = "exceedance")
}
}
\seealso{
\code{\link[=analyte_summary]{analyte_summary()}} for the per-analyte summary of the same round,
\code{\link[=select_max_concentration]{select_max_concentration()}} to collapse duplicates beforehand, and
\code{\link[=create_gt]{create_gt()}} to format the result.
}
