% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyte_summary.R
\name{analyte_summary}
\alias{analyte_summary}
\title{Per-analyte summary of a monitoring round}
\usage{
analyte_summary(
  data,
  round_col = NULL,
  round = NULL,
  group_vars = NULL,
  criteria_col = criteria,
  include_criteria = NULL,
  save_path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{chemistry tibble from \code{\link[=data_processor]{data_processor()}}, usually with a
guideline set joined on by \code{\link[=join_action_levels]{join_action_levels()}}.}

\item{round_col}{name of the column naming the monitoring round. Can be
given with or without quotes. By default the first of \code{monitoring_round},
\code{task_code}, \code{date} or \code{sampled_date_time} that \code{data} carries.}

\item{round}{the round to report on. Default \code{NULL}, the latest round
present - resolved by the latest date sampled within each round where the
round is recorded as text, so \code{"2025 Q10"} is not read as earlier than
\code{"2025 Q9"}.}

\item{group_vars}{character vector of further columns to group by, e.g.
\code{"monitoring_zone"}. Analytes are always grouped by \code{chem_name}, and by
\code{output_unit} and \code{criteria_set} where those are present, so results
reported in two units are never pooled.}

\item{criteria_col}{name of the column holding the guideline value, matching
the \code{value_col} it was joined under by \code{\link[=join_action_levels]{join_action_levels()}}. Can be
given with or without quotes. Default \code{criteria}.}

\item{include_criteria}{include the guideline and the exceedance columns.
Default \code{NULL}, which includes them when \code{criteria_col} is present.}

\item{save_path}{full file path, including filename, to write the table to,
e.g. \code{"output/round-summary.xlsx"}. The directory is created if it does
not exist.}

\item{quiet}{suppress the message naming the round that was reported on.}
}
\value{
A tibble with one row per analyte (and per \code{group_vars},
\code{output_unit} and \code{criteria_set} where present), carrying \code{n_samples},
\code{n_detects}, \code{pct_detects}, \code{min_prefix}, \code{min_conc}, \code{max_prefix},
\code{max_conc} and \code{max_location}, plus - with \code{include_criteria} - the
guideline value, \code{n_exceedances}, \code{n_exceedance_locations} and
\code{exceedance_locations}. The round reported on is attached as a \code{"round"}
attribute. Format it for a report with \code{\link[=create_gt]{create_gt()}}.
}
\description{
One row per analyte for a single monitoring round: how many samples were
taken, how many detected the analyte, the range of concentrations, where the
maximum came from, and - where a guideline set has been joined on by
\code{\link[=join_action_levels]{join_action_levels()}} - which locations exceeded it.
}
\details{
Distinct from \code{\link[=summary_stats]{summary_stats()}}, which is one row per location \emph{and}
chemical across the whole record, with percentiles. This is the
"what did this round find, and where did it exceed" table.

Two conventions are worth stating, because they decide what the table says:
\itemize{
\item The \strong{maximum} reported is the highest \emph{detected} result. Where nothing
was detected there is no detection to report, so the highest result is
used instead and its \code{<} is carried through in \code{max_prefix} rather than
the row coming out blank. The \strong{minimum} is the lowest result of the
round, detected or not.
\item An \strong{exceedance} is whatever \code{\link[=join_action_levels]{join_action_levels()}} decided it was. That
function settles once - through its \code{lor_as_exceedance} argument - whether
a non-detect whose limit of reporting sits above the guideline counts, and
the verdict is counted here as it stands. A \code{criteria} column added by
hand carries no verdict, and falls back to detected results above the
guideline.
}

Where an analyte has no guideline at all, the exceedance columns are \code{NA}
rather than \code{0}: a \code{0} means a guideline applies and nothing exceeded it.
}
\examples{
analyte_summary(gRs_data)

\dontrun{
chem <- data_processor("davLChem1_Chemistry.xlsx")
anzg <- action_level_processor("ANZG 95 Marine.xlsx", name = "ANZG 95\%")
compared <- join_action_levels(chem, anzg)

analyte_summary(compared)
analyte_summary(compared, group_vars = "monitoring_zone")

# one row per analyte per guideline set
compared \%>\%
  join_action_levels(anzg_99, value_col = "criteria_99") \%>\%
  criteria_long() \%>\%
  analyte_summary()

# formatted for a report
analyte_summary(compared) \%>\% create_gt()
}
}
\seealso{
\code{\link[=summary_stats]{summary_stats()}} for per-location statistics across the whole
record, \code{\link[=join_action_levels]{join_action_levels()}} for the guideline join this reads, and
\code{\link[=create_gt]{create_gt()}} to format the result.
}
