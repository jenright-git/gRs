% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/action_levels.R
\name{criteria_long}
\alias{criteria_long}
\title{Stack several joined guideline sets into one criteria column}
\usage{
criteria_long(data, sets = NULL, keep_unmatched = TRUE)
}
\arguments{
\item{data}{chemistry tibble with one or more guideline sets joined onto it
by \code{\link[=join_action_levels]{join_action_levels()}}.}

\item{sets}{names of the value columns to stack, e.g.
\code{c("criteria", "criteria_99")}. Defaults to every set found.}

\item{keep_unmatched}{keep the rows where a set found no guideline. Default
\code{TRUE}, so a result missing from one set is still visible as a gap rather
than dropping out of the table. \code{FALSE} returns comparisons only.}
}
\value{
\code{data} with the per-set columns replaced by \code{criteria_set} (the
column the guideline came from), \code{criteria_name} (the guideline set's
name), \code{criteria}, \code{criteria_unit}, \code{criteria_basis},
\code{exceedance_ratio}, \code{exceedance} and \code{lor_above_criteria}. Row count is
\code{nrow(data)} times the number of sets, less any dropped by
\code{keep_unmatched = FALSE}.
}
\description{
\code{\link[=join_action_levels]{join_action_levels()}} puts each guideline set in its own column, which is
the shape to read a table in but the wrong one to analyse across sets. This
stacks them: every result appears once per guideline set, with a single
\code{criteria} column and a single \code{exceedance} column whichever set it came
from, so one \code{group_by(criteria_set)} covers the lot.
}
\details{
The sets are found from the columns \code{\link[=join_action_levels]{join_action_levels()}} writes - a value
column with matching \verb{_name}, \verb{_unit} and \verb{_basis} columns beside it - so
no bookkeeping is needed beyond having joined them.
}
\examples{
\dontrun{
compared <- chem \%>\%
  join_action_levels(anzg_95) \%>\%
  join_action_levels(anzg_99, value_col = "criteria_99")

long <- criteria_long(compared)

long \%>\%
  dplyr::group_by(criteria_set, chem_name) \%>\%
  dplyr::summarise(exceedances = sum(exceedance, na.rm = TRUE))
}
}
\seealso{
\code{\link[=join_action_levels]{join_action_levels()}}, which produces the wide form this reads.
}
