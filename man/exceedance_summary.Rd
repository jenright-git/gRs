% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exceedance_summary.R
\name{exceedance_summary}
\alias{exceedance_summary}
\title{The results that exceeded their guideline}
\usage{
exceedance_summary(
  data,
  round_col = NULL,
  round = NULL,
  criteria_col = criteria,
  include_lor = FALSE,
  save_path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{chemistry tibble with a guideline set joined on by
\code{\link[=join_action_levels]{join_action_levels()}}.}

\item{round_col}{name of the column naming the monitoring round. Can be
given with or without quotes. By default the first of \code{monitoring_round},
\code{task_code}, \code{date} or \code{sampled_date_time} that \code{data} carries.}

\item{round}{the round to report on. Default \code{NULL}, the latest round
present.}

\item{criteria_col}{name of the column holding the guideline value, matching
the \code{value_col} it was joined under by \code{\link[=join_action_levels]{join_action_levels()}}. Can be
given with or without quotes. Default \code{criteria}.}

\item{include_lor}{also return the non-detects whose limit of reporting sits
above the guideline. Default \code{FALSE}.}

\item{save_path}{full file path, including filename, to write the rows to.
The directory is created if it does not exist.}

\item{quiet}{suppress the messages naming the round and counting what was
found.}
}
\value{
\code{data}'s exceeding rows, with the reporting columns moved to the
front and all others kept, ordered by analyte and then by
\code{exceedance_ratio} descending. A round in which nothing exceeded returns a
table with no rows rather than \code{NULL}. The round is attached as a
\code{"round"} attribute and the per-analyte exceeding locations as a
\code{"locations"} attribute.
}
\description{
Every result in a monitoring round that exceeded the guideline joined onto
it by \code{\link[=join_action_levels]{join_action_levels()}}, one row per exceeding result, worst first
within each analyte. This is the record behind the counts
\code{\link[=analyte_summary]{analyte_summary()}} reports and the flags \code{\link[=historical_range]{historical_range()}} sets: all
three read the same \code{exceedance} column, so no table can report an
exceedance another does not.
}
\details{
Whether a non-detect whose limit of reporting sits above the guideline
counts as an exceedance was settled once by \code{\link[=join_action_levels]{join_action_levels()}}, through
its \code{lor_as_exceedance} argument. It is not re-decided here. Those results
are worth reporting in their own right, though - a limit of reporting too
high to demonstrate compliance is a finding, not a pass - so
\code{include_lor = TRUE} adds them, told apart by the \code{lor_above_criteria}
column. Doing so means the row count no longer matches
\code{\link[=analyte_summary]{analyte_summary()}}'s \code{n_exceedances}, which counts exceedances alone.

A named list of the exceeding locations for each analyte is attached as a
\code{"locations"} attribute, for writing inline text. Every analyte that had a
guideline in the round appears in it, so an analyte that exceeded nothing
gives \code{character(0)} rather than \code{NULL} and inline code does not have to
guard against a missing name.
}
\examples{
\dontrun{
chem <- data_processor("davLChem1_Chemistry.xlsx")
anzg <- action_level_processor("ANZG 95 Marine.xlsx", name = "ANZG 95\%")
compared <- join_action_levels(chem, anzg)

exceeded <- exceedance_summary(compared)
exceeded \%>\% create_gt()

# for inline text: "PFOS exceeded the guideline at MW01, MW06 and MW07."
locs <- attr(exceeded, "locations")
locs[["Perfluorooctane sulfonic acid (PFOS)"]]

# limits of reporting too high to demonstrate compliance, reported with them
exceedance_summary(compared, include_lor = TRUE)

# one guideline set at a time where several are stacked
compared \%>\%
  criteria_long() \%>\%
  dplyr::filter(criteria_set == "criteria_99") \%>\%
  exceedance_summary()
}
}
\seealso{
\code{\link[=analyte_summary]{analyte_summary()}} for the per-analyte counts of the same
exceedances, \code{\link[=join_action_levels]{join_action_levels()}} for the comparison this reads, and
\code{\link[=create_gt]{create_gt()}} to format the result.
}
