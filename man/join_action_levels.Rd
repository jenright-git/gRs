% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/action_levels.R
\name{join_action_levels}
\alias{join_action_levels}
\title{Join action levels onto a chemistry table}
\usage{
join_action_levels(
  chem_data,
  action_levels,
  value_col = "criteria",
  match_matrix = TRUE,
  match_fraction = TRUE,
  match_name = TRUE,
  lor_as_exceedance = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{chem_data}{chemistry tibble from \code{\link[=data_processor]{data_processor()}}.}

\item{action_levels}{action level tibble from \code{\link[=action_level_processor]{action_level_processor()}}.
Must hold a single guideline set; filter it or call this function once
per set (with a different \code{value_col}) to compare several.}

\item{value_col}{name of the column the guideline value is written to.
Default \code{"criteria"}, which is what the rest of the package expects.
Give a different name to hold a second guideline set alongside the first.}

\item{match_matrix}{compare \code{matrix_code} before matching. Default \code{TRUE}.}

\item{match_fraction}{compare fraction and leachate status before matching.
Default \code{TRUE}.}

\item{match_name}{fall back to matching on \code{chem_name} where \code{chem_code}
found nothing. Default \code{TRUE}.}

\item{lor_as_exceedance}{count a non-detect whose limit of reporting sits
above the guideline as an exceedance. Default \code{FALSE} - a non-detect says
the analyte was not seen, not that it was present at the LOR. Set \code{TRUE}
where an LOR above the guideline is a failure in its own right. Either
way the two are told apart by \code{lor_above_criteria}.}

\item{quiet}{suppress the matching report. Default \code{FALSE}.}
}
\value{
\code{chem_data} with the guideline columns added: \verb{<value_col>} (the
guideline in the result's own unit), \verb{<value_col>_name},
\verb{<value_col>_unit}, \verb{<value_col>_basis}, and the comparison columns
\code{exceedance_ratio}, \code{exceedance} and \code{lor_above_criteria}. Joining a
second set under a different \code{value_col} prefixes its comparison columns
with that name (\code{criteria_99_exceedance} and so on) so the two sets sit
side by side without overwriting each other; \code{\link[=criteria_long]{criteria_long()}} stacks
them into one column. Row count and order are unchanged. The analytes
that found no guideline are attached as an \code{"unmatched"} attribute.
}
\description{
Adds a guideline value to every result in a chemistry table returned by
\code{\link[=data_processor]{data_processor()}}, converted into the unit each result is reported in, so
concentrations and guidelines can be compared directly. The added
\code{criteria} column is the one \code{\link[=summary_stats]{summary_stats()}} reads when
\code{include_criteria = TRUE} and the one \code{\link[=timeseries_plot]{timeseries_plot()}} draws when passed
as \code{criteria_col}.
}
\details{
Analytes are matched on \code{chem_code} - the CAS number - which is distinct
and unique per analyte and is never reduced, stripped or otherwise
collapsed to force a join. A code carrying a disambiguating suffix names a
different analyte from the bare code, so \code{91-20-3} (naphthalene by SVOC)
and \verb{91-20-3_VOC} (naphthalene by VOC) keep their separate guidelines and
neither stands in for the other. A code that finds no guideline is reported
as unmatched rather than being trimmed until it matches something.

Where a result carries no code at all, \code{chem_name} is tried instead - case-
and whitespace-insensitively, ignoring the \code{Dissolved} prefix that
\code{\link[=data_processor]{data_processor()}} adds to filtered results. This is what matches an EQuIS
export reporting names against codes the guideline set does not share. Pass
\code{match_name = FALSE} to match on the code alone.

The report printed at the end says how many results matched on each.

A result is only compared against a guideline that applies to it:
\itemize{
\item \strong{Matrix} - \code{matrix_code} must agree, after normalising the ESDAT and
EQuIS spellings (\code{WATER}/\code{Water}, \code{Soil}/\code{SEDIMENT}). A matrix left
unrecorded on either side matches anything, so a guideline set carrying
no \code{matrix_code} at all is warned about rather than quietly compared
across matrices.
\item \strong{Fraction} - a filtered (dissolved) result needs \code{filtered = TRUE}, a
total result needs \code{total = TRUE}. A result whose fraction is unrecorded
(\code{NA}, or EQuIS's \code{"N"}) accepts either.
\item \strong{Leachate} - a \code{LEACHED_REG} result needs \code{leached = TRUE}, and an
ordinary result needs \code{leached = FALSE}.
\item \strong{Units} - the guideline is converted into the result's own unit.
Conversion is only attempted within a dimension (concentration in a
liquid, concentration in a solid); \code{ppm} and \code{ppb} are read against
whichever dimension the other unit belongs to. A guideline that cannot be
converted is dropped rather than compared, and reported.
}

A guideline carrying no unit at all is never joined and never assumed to
share the result's unit - the unit is missing from the source file and that
is a fault to be fixed there, not guessed at here. It is reported apart
from the guidelines that carried a unit that would not convert, since the
two call for different fixes. A guideline set with no units anywhere is an
error rather than a table of empty columns.

Where more than one guideline still applies to a result, the lowest is
used and the ambiguity is reported.

Only a detected result exceeds a guideline. A non-detect reported above the
guideline - an LOR too high to demonstrate compliance - is recorded
separately in \code{lor_above_criteria}, and counts as an exceedance only where
\code{lor_as_exceedance = TRUE}. That choice is made here, once, so that every
function reading \code{exceedance} afterwards agrees with it.
}
\examples{
\dontrun{
chem <- data_processor("davLChem1_Chemistry.xlsx")
anzg <- action_level_processor("ANZG 95 Marine.xlsx", name = "ANZG 95\%")

compared <- join_action_levels(chem, anzg)

# what did not match
attr(compared, "unmatched")

# feeds straight into the rest of the package
summary_stats(compared, include_criteria = TRUE)
timeseries_plot(compared, criteria_col = criteria)

# a second guideline set alongside the first, then stacked for analysis
compared <- compared \%>\%
  join_action_levels(anzg_99, value_col = "criteria_99")

long <- criteria_long(compared)
}
}
\seealso{
\code{\link[=criteria_long]{criteria_long()}} to stack several joined sets into one column.
}
