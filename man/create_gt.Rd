% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_gt.R
\name{create_gt}
\alias{create_gt}
\title{Format a summary table for a report}
\usage{
create_gt(
  data,
  decimals = 5,
  missing_text = "",
  labels = NULL,
  width = NULL,
  round_note = TRUE,
  highlight = TRUE,
  merge_range = TRUE
)
}
\arguments{
\item{data}{a tibble, typically from \code{\link[=analyte_summary]{analyte_summary()}}.}

\item{decimals}{decimal places for concentration columns. Default 5, with
trailing zeros dropped.}

\item{missing_text}{text shown in place of a missing value. Default \code{""}.}

\item{labels}{named character vector of column labels, e.g.
\code{c(max_location = "Where")}, merged over the built-in dictionary. Where a
prefix/concentration pair has been merged, label the \verb{_prefix} column -
that is the name the merged column keeps.}

\item{width}{table width passed to \code{gt::tab_options(table.width = )}, e.g.
\code{1000} or \code{gt::pct(100)}. Default \code{NULL}, leaving it to \code{gt}.}

\item{round_note}{add a source note naming the monitoring round the table
reports on, read from the \code{"round"} attribute \code{\link[=analyte_summary]{analyte_summary()}} and
\code{\link[=historical_range]{historical_range()}} attach. Default \code{TRUE}; ignored where the table
carries no such attribute.}

\item{highlight}{mark the findings on the cells they describe, and hide the
flag columns that have been said. Default \code{TRUE}. \code{FALSE} leaves the table
unstyled, with the flags shown as columns. Only the rules whose columns
are present are applied, so this is safe on any table.}

\item{merge_range}{read a \verb{<x>min_prefix} / \verb{<x>max_prefix} pair as the one
range it describes, so \code{historical_range()}'s two history columns become
\code{0.5 - 8} under "Historical Range" and \code{analyte_summary()}'s become
"Concentration Range". Default \code{TRUE}. \code{FALSE} reports the two ends in
columns of their own. Name the merged column by labelling the minimum,
which is the name it keeps. Where only one end has anything to report -
a group holding a single result, or one whose only history is a
non-detect - the cell reads that end alone rather than trailing a
separator across nothing.}
}
\value{
A \code{gt_tbl}.
}
\description{
Turns a table from \code{\link[=analyte_summary]{analyte_summary()}} - or any table sharing its column
names - into a formatted \code{gt} table ready to print in a Quarto or R Markdown
document. The counterpart of \code{\link[=create_dt]{create_dt()}}, which produces an interactive
table for a web page.
}
\details{
Everything is decided from the column names, so no arguments are needed for
the common case:
\itemize{
\item every \verb{<x>_prefix} / \verb{<x>_conc} pair is merged into a single cell, so a
non-detect reads \verb{<0.05} rather than filling two columns, and a minimum
and maximum are then merged into the range they span - see \code{merge_range};
\item concentration columns are formatted to \code{decimals} places with trailing
zeros dropped, count columns (\verb{n_*}) as integers, and percentage (\verb{pct_*})
and ratio (\verb{*_ratio}) columns to one place;
\item missing values are blanked, or replaced with \code{missing_text};
\item columns are labelled from a shared dictionary - \code{chem_name} becomes
"Analyte", \code{n_samples} becomes "Samples (n)", and so on - and everything
is right-aligned;
\item the monitoring round the table reports on is added as a source note,
where the table carries one;
\item the findings are marked on the cell they describe - see \code{highlight}.
}

Read down a column of \code{TRUE}s and \code{0}s, a new maximum or an exceedance does
not stand out. Said on the value itself it does, so \code{highlight} styles the
cell rather than leaving the reader to find the flag that describes it:
\itemize{
\item a \strong{new maximum} is bold, a \strong{new minimum} italic and underlined, and a
\strong{spike} shaded, all on the current concentration, with a footnote saying
what the shading means. The \code{new_max}, \code{new_min} and \code{spike} columns are
then hidden, having been said - whether or not the round tripped them, so
the table keeps one shape from round to round;
\item an \strong{exceedance} is bold, on the count or verdict itself, for every
guideline set the table carries. Those columns are kept: how many exceeded
is worth reading, not just that some did.
}

A guideline set joined under its own \code{value_col} names its columns after
itself - \code{criteria_99}, \code{criteria_99_n_exceedances} - and those are read as
the set's columns and labelled as though it had been joined under the
default name. Where a table carries two sets - two summaries bound side by
side, or two sets joined onto the same results - each set's columns are put
under a spanner naming the set, so "Criteria" appearing twice is not
ambiguous. Name the sets by labelling their value columns, e.g.
\code{labels = c(criteria = "ANZG 95\%", criteria_99 = "ANZG 99\%")}.

Requires the \code{gt} package, which is not installed with gRs.
}
\examples{
\dontrun{
analyte_summary(compared) \%>\% create_gt()

analyte_summary(compared) \%>\%
  create_gt(decimals = 3, labels = c(max_location = "Peak Location"))

# new maximums bold, new minimums underlined, spikes shaded
historical_range(compared) \%>\% create_gt()

# the same table with the flags left as columns and nothing styled
historical_range(compared) \%>\% create_gt(highlight = FALSE)

# the history reported as two columns rather than one range
historical_range(compared) \%>\% create_gt(merge_range = FALSE)

# two guideline sets bound side by side: each block under a spanner
dplyr::left_join(
  analyte_summary(compared),
  analyte_summary(compared, criteria_col = criteria_99),
  by = c(
    "chem_name", "output_unit", "n_samples", "n_detects", "pct_detects",
    "min_prefix", "min_conc", "max_prefix", "max_conc", "max_location"
  )
) \%>\%
  create_gt(labels = c(criteria = "ANZG 95\%", criteria_99 = "ANZG 99\%"))
}
}
\seealso{
\code{\link[=create_dt]{create_dt()}} for an interactive table, \code{\link[=analyte_summary]{analyte_summary()}} for the
table this formats.
}
