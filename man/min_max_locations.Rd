% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/min_max_locations.R
\name{min_max_locations}
\alias{min_max_locations}
\title{The highest and lowest results of a monitoring round, and where they came
from}
\usage{
min_max_locations(
  data,
  n_max = 1,
  n_min = 1,
  round_col = NULL,
  round = NULL,
  group_vars = NULL,
  with_ties = FALSE,
  save_path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{chemistry tibble from \code{\link[=data_processor]{data_processor()}}.}

\item{n_max}{how many results to report from the top of each analyte's
range. Default 1. \code{0} reports the minima alone.}

\item{n_min}{how many results to report from the bottom. Default 1. \code{0}
reports the maxima alone.}

\item{round_col}{name of the column naming the monitoring round. Can be
given with or without quotes. By default the first of \code{monitoring_round},
\code{task_code}, \code{date} or \code{sampled_date_time} that \code{data} carries.}

\item{round}{the round to report on. Default \code{NULL}, the latest round
present - resolved by the latest date sampled within each round where the
round is recorded as text, so \code{"2025 Q10"} is not read as earlier than
\code{"2025 Q9"}.}

\item{group_vars}{character vector of further columns to rank within, e.g.
\code{"monitoring_zone"} for the highest and lowest result of each zone.
Analytes are always grouped by \code{chem_name}, and by \code{output_unit} and
\code{criteria_set} where those are present.}

\item{with_ties}{keep every result tied with the last one selected, so an
analyte whose two highest results are equal reports both. Default \code{FALSE},
which breaks the tie on \code{location_code} and returns the number asked for.}

\item{save_path}{full file path, including filename, to write the rows to,
e.g. \code{"output/round-extremes.xlsx"}. The directory is created if it does
not exist.}

\item{quiet}{suppress the messages naming the round and counting what was
found.}
}
\value{
\code{data}'s selected rows, with \code{extreme} (\code{"Maximum"} or
\code{"Minimum"}) and \code{rank} added, the reporting columns moved to the front
and all others kept. Ordered by analyte, then maxima before minima, then
by rank. The round reported on is attached as a \code{"round"} attribute.
Format it for a report with \code{\link[=create_gt]{create_gt()}}.
}
\description{
The two ends of each analyte's range in a single monitoring round, one row
per selected result, carrying the location, the sample and everything else
the result was reported with. Where \code{\link[=analyte_summary]{analyte_summary()}} reduces each analyte
to a single row and names only the location the maximum came from, this
returns the results themselves, as many from each end as are asked for.
}
\details{
The conventions are the ones \code{\link[=analyte_summary]{analyte_summary()}} follows, so the two tables
cannot disagree about what the highest result of a round was:
\itemize{
\item The \strong{maximum} is the highest \emph{detected} result. Where nothing was
detected there is no detection to report, so the highest result stands in
and carries its \code{<} through in \code{prefix} rather than the row coming out
blank.
\item The \strong{minimum} is the lowest result of the round, detected or not, so a
non-detect can legitimately be reported as one.
}

Results are ranked within each analyte, and within \code{output_unit} and
\code{criteria_set} where the table carries them, so results reported in two
units are never ranked against each other. \code{rank} is \code{1} for the highest
result of the \code{"Maximum"} rows and the lowest of the \code{"Minimum"} rows;
results of equal concentration share a rank.

Asking for more results than an analyte holds returns what it has, so a
group with a single result reports that result at both ends of its range.
That is the range the record shows rather than an error, but it is worth
reading \code{n_max} and \code{n_min} as a ceiling rather than a promise.
}
\examples{
# the highest and the lowest result of each analyte, and where each was
min_max_locations(gRs_data)

# the peak, and the four cleanest locations behind it
min_max_locations(gRs_data, n_max = 1, n_min = 4)

\dontrun{
chem <- data_processor("davLChem1_Chemistry.xlsx")

# the highest and lowest result of each zone, formatted for a report
chem \%>\%
  min_max_locations(group_vars = "monitoring_zone") \%>\%
  create_gt()
}
}
\seealso{
\code{\link[=analyte_summary]{analyte_summary()}} for the per-analyte summary these results are
the ends of, \code{\link[=historical_range]{historical_range()}} to compare the round against the record
behind it, and \code{\link[=create_gt]{create_gt()}} to format the result.
}
